<%= form_for @event do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <%= f.text_field :location %>
  <%= f.date_field :date, prefix: DateTime.now.to_date%>
  <%= f.number_field :limit %>
  <%= f.submit "Oluştur"%>
<% end %>
